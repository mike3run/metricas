.row
  .col-md-9
    h1.mb-3 Costo Promedio de Retención
  .col-md-3
    img(src='assets/images/upaep.png' alt="Logo de la UPAEP")
.row
  .col-md-4
    form(novalidate)
      .form-group
        label.block Gastos de Retención
          .input-group
            span.input-group-addon $
            input.form-control(type='number', placeholder='Gastos de Retención', ng-model="ctrl.gastos")
      .form-group
        label.block Número de Clientes Retenidos
          .input-group
            span.input-group-addon #
            input.form-control(type='number', placeholder='Número de Clientes Retenidos', ng-model="ctrl.numero")

      button.btn.btn-info(type='submit', ng-click="ctrl.cpr(ctrl.gastos, ctrl.numero)")
        | Calcular

  .offset-md-3.col-md-5
    .card.card-block(ng-class="{'card-info' : ctrl.respuesta, 'card-inverse': ctrl.respuesta }")
      h4.card-title Costo Promedio
      p.card-text(ng-if="ctrl.respuesta") {{ ctrl.respuesta | number:2 }}

    .card.card-block
      h4.card-title Fórmula
      p.card-text Costo promedio de rentención ($)= gastos de retención ($) /número de clientes retenidos (#)


    .card.card-block
      h4.card-title ¿Qué es?
      p.card-text Costo promedio de rentención: El radio de lo que se gasta en retencion en relacion con el número de clientes retenidos
      p.card-text Gastos de retención: Cantidad de dinero que se invirtió para retener a un cliente
      p.card-text Número de clientes retenidos: La cantidad de clientes que se quedaron contigo
